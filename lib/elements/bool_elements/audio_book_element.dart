import 'package:flutter/material.dart';

class AudioBookElement extends StatelessWidget {
  const AudioBookElement({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
