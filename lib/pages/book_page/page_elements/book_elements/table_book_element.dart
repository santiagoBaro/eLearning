import 'package:flutter/material.dart';

class TableBookElement extends StatelessWidget {
  const TableBookElement({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
